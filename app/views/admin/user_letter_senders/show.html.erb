
$("#admin_letter_form").replaceWith("<%= j render('admin/user_letter_senders/form', from_user: current_user, to_user: @user, letter: @letter) %>")
$("#button").click()
$('#conversation_recipients').tokenInput('/ajax/users',{theme: 'facebook',prePopulate: $('#conversation_recipients').data('load')})

