<h1><%= t(".title") %></h1>

<%= link_to edit_icon, edit_admin_setting_path(@admin_setting)  if @admin_setting%>
<%= link_to plus_icon, new_admin_setting_path(@admin_setting)  if @admin_setting.nil?%>

<% if  @admin_setting && @admin_setting.album %>

<table class="table table-striped">
  
  <tbody>
    
      <tr>
        <td>Альбом</td>
        <td><%= @admin_setting.album.title %></td>
      </tr>
      <tr>
        <td>Новость</td>
        <td><%= @admin_setting.novelty.title %></td>
      </tr>
    
  </tbody>
</table>

<% end  %>

<br>
