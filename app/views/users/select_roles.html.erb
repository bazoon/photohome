

<h3>Choose roles</h3>


<%= simple_form_for :users, :url => users_path, method: :get do |f| %>
   
    <div class="form-group">
      <%= f.input :role_id, selected: 1, label: "Role", collection: @roles,  :include_blank => "Без роли", label_method: :name, value_method: :id, :input_html => { class: "form-control"} %>
    </div>

  <%= f.button :submit, value: "Submit", class: "btn btn-default" %>



<% end %>
