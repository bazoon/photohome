<ul class="list-unstyled">

<% breadcrumb :authors %>

<% @users.each do |user| %>

<li>
	<%= image_tag(user.avatar_url(:thumb),class: "img-rounded") %>
	<%= link_to user.full_name,user_profile_path(user) %>
  
</li>

<% end  %>

</ul>