<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Photohome" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Photohome" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  

  </head>

  <body>

    <div class="navbar navbar-inverse" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><%= I18n.t("project_name") %></a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
           
            <%= render 'layouts/common_menu' %>
            <%= render 'layouts/moder_menu' if current_user && current_user.is_stuff? %>
            <%= render 'layouts/admin_menu' if current_user && current_user.admin? %>
            <%= render 'layouts/navigation' %>
            <%= render 'layouts/lang_select' %>
          
          </ul>
          
        </div><!--/.navbar-collapse -->
      </div>
    </div>

    <div class="wrapper">

    <div class="container">
     <%=render 'layouts/messages'%>
      <%= yield %>
    </div>

      <hr>

    <div class="push"><!--//--></div>
    </div>

      <footer>
        <p>&copy; Company 2013</p>
      </footer>
   



    

  </body>
</html>
