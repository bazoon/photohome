<div class="content row">
  <div class="col-lg-12">
    <header class="clearfix">
    
      <section id="branding">
      

        <%= link_to image_tag("logo1.jpg"),root_path %>
     

      
      </section><!-- branding -->

      <section class="navbar navbar-inverse" >
    
        
        <div class="navbar-header">

          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

     
        </div>

         <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <%= render 'layouts/photo_menu' %>
            <%= render 'layouts/common_menu' %>
            <%= render 'layouts/admin_menu' if current_user && (current_user.is_stuff? || current_user.is_writer?) %>
            <%= render 'layouts/navigation' %>
            
          </ul>
          <ul class="nav navbar-nav navbar-right">
           <%= render 'layouts/age_agreement_button' %>
            <%= render 'layouts/lang_select' %>
          </ul>
         </div>
   
    </section> 


    </header><!-- header -->
  </div><!-- column -->
</div><!-- content -->