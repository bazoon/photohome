<%= render partial: "image", locals: {photo: @photo } %>
<%= render partial: "comment/comments", locals: {resource: @photo } %>
<%= render partial: "comment/form", locals: { user: @user, resource: @photo} if can? :write, Comment %>
