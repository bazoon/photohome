<%- unless current_user && current_user.is_stuff? %>
<div id="top_banners" style="padding-bottom: 10px" >
  
 <%- banner =  BannerGenerator.new.top %> 
 <%= link_to image_tag(banner.image_url), banner.link ,:target => "_blank" if banner%>

</div>
<% end %>

